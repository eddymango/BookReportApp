import 'package:flutter/material.dart';

class AddBookReport extends StatelessWidget {
  const AddBookReport({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: const Text("책 등록 하기"),
        ),
        body: Column(
          children: [],
        ),
      ),
    );
  }
}
